#include <ctime>
#include <cstdlib>

srand(time(0));

float random(int n = RAND_MAX, int m=RAND_MAX) {
    float random = static_cast<float>(rand())/RAND_MAX;
    if (n != m && m != RAND_MAX)
        
}
